# strongdog_prox

This is a test project to demonstrate the injection of games from `cards-data.js` using `strongdog.com` as the base URL, now proxied through `corsproxy.io`.

## How to use

Open `index.html` in a web browser to see the list of games. All game and image links will now be routed through `https://corsproxy.io/?`.